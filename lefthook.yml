pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,vue}"
      run: pnpm eslint {staged_files}
      stage_fixed: true

    format:
      glob: "*.{css,html,json,md,yaml,yml}"
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
